<Project>
    <PropertyGroup>
        <BazelWorkspaceName>rules_msbuild</BazelWorkspaceName>
        <BazelWorkspacePath>$(MSBuildThisFileDirectory)</BazelWorkspacePath>
    </PropertyGroup>
</Project>