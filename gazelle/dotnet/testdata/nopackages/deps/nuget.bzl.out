load("@rules_msbuild//dotnet:defs.bzl", "nuget_fetch")

def nuget_deps():
    nuget_fetch(
        name = "nuget",
        packages = {
        },
        target_frameworks = ["net5.0"],
        use_host = True,
    )
