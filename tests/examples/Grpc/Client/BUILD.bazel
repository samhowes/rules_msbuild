load("@rules_msbuild//dotnet:defs.bzl", "msbuild_binary")

msbuild_binary(
    name = "Client",
    srcs = ["Program.cs"],
    target_framework = "net5.0",
    visibility = ["//visibility:public"],
    deps = [
        "//tests/examples/Grpc/protos:Proto.Net",
        "@nuget//Grpc.Net.Client",
    ],
)
